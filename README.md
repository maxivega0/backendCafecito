````
 npm install -g nodemon
 npm install express --save ["Infraestructura web rapida, minimalista y flexible"]
 npm i babel-cli babel-preset-env express-validator mongoose cors morgan dotenv
 

babel - "ayudan a transformar el codigo js a la version que entiende node"

````
